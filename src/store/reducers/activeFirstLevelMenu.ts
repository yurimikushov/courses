import { TopLevelCategory } from '../../enums'

const initialState = TopLevelCategory.Courses

const activeFirstLevelMenuReducer = (
  state = initialState
): TopLevelCategory => {
  return state
}

export { activeFirstLevelMenuReducer }
